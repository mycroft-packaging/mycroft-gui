#!/usr/bin/make -f

%:
	dh $@ --with=kf5,pkgkde-symbolshelper --parallel

override_dh_auto_configure:
	dh_auto_configure -Skf5 -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DKDE_INSTALL_LIBDIR=/lib -DKDE_INSTALL_USE_QT_SYS_PATHS=ON

override_dh_auto_install:
	dh_auto_install
